// should match file names
export enum ROUTES {
  home = '/',
  alarm = '/alarm',
  arrowClock = '/arrow-clock',
  cssJs = '/css-js',
  videoPlayer = '/video-player',
  drawOnCanvas = '/draw-on-canvas',
  edm = '/edm',
  speechRecognition = '/speech-recognition',
  webSQL = '/web-sql'
}
